{% extends 'base.html.twig' %}

{% block title %}Design patterns{% endblock %}

{% block pagetitle %}Homepage{% endblock %}

{% block body %}
<section class='py-3 flex flex-col lg:flex-row'>
<div class='py-4 lg:w-2/3 lg:pr-6' >
    <h1 class='text-lg text-primary font-bold font-Plex uppercase pb-2'>Project description</h1>
    <p class='pt-4'>This project contains <b class='text-primary'>23 classic design patterns</b> for object oritented programming.</p>
    <p>The application was written in Symfony 5 (PHP framework) by Lucian Turlea and can be downloaded!</p>
    <p>Some of the patterns may need advance knowledge of the framework's form utilities. Other patterns like <b class='text-primary'>Abstrct factory</b> have a straight forward implementation.</p>
    <p>Application has <b class='text-primary'>no database</b> included, or needed. The project uses classes with constants and it is ready after the download!</p>
</div>
<div class='py-4 lg:w-1/3 flex flex-col gap-3 lg:pl-6'>
    <h2 class='text-primary  font-Plex uppercase pb-2'>Technologies</h2>
    <div class='flex gap-2 font-Plex'>
         <img src="/img/symfony.svg" style='width:30px' class='rounded-lg ' alt="symfony">
        <span>Symfony 5.3</span>
    </div>
    <div class='flex gap-2 items-center font-Plex'>
        <img src="/img/tailwindcss.svg" style='width:30px' class='rounded-lg ' alt="tailwindcss">
        <span>Tailwind 3</span>
    </div>
     <div class='flex gap-2 items-center font-Plex'>
        <img src="/img/sass.svg" style='width:29px' class='rounded-lg pl-2' alt="sass">
        <span>SASS</span>
    </div>
    <div class='flex gap-2 items-center font-Plex'>
        <img src="/img/twig.png" style='width:29px' class='rounded-lg pl-2' alt="twig">
        <span>Twig</span>
    </div>

    
</div>
</section>
{# md:px-14 lg:px-24 #}
<section class='py-16 border-stone-50 border-t-2'>
       <h1 class="text-lg text-primary font-bold font-Plex uppercase pb-2">Check all 23 patterns</h1>
        <div class='flex mx-auto w-full flex flex-wrap pt-4'>
            {% for pattern in twig_patterns.getList %}
                <div class='flex w-full lg:w-1/2 pb-12 lg:pr-12 '>
                    <a href="{{ path(pattern.route)  }}" class='w-full rounded-lg'>
                    <div class="card-img flex flex-row w-full gap-4 border-stone-100 border-2 rounded-lg hover:text-stone-600 transition-all">
                        <div class='flex flex-col'>
                            <img src="{{ pattern.img }}" style='width:150px' class='min-w-fit rounded-lg max-w-none' alt="{{ pattern.img }}">
                        </div>
                        <div class='flex flex-col gap-2 my-4 pr-2'>
                            <span class='uppercase'> {{ pattern.name }} </span>
                            <span class=''> {{ pattern.title }} </span>
                        </div>
                    </div>
                    </a>
                </div>
            {% endfor %}    
        </div> 

</section>
{% endblock %}
